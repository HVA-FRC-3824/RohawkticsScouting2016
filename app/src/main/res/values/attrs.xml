<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="CustomScoutView">
        <attr name="label" format="string"/>
        <attr name="key" format="string"/>
    </declare-styleable>

    <declare-styleable name="CustomSpinner">
        <attr name="spinner_values" format="reference"/>
        <attr name="spinner_defaultValue" format="string"/>
    </declare-styleable>

    <declare-styleable name="CustomRadioButtons">
        <attr name="radio_values" format="reference"/>
        <attr name="radio_defaultValue" format="string"/>
    </declare-styleable>
</resources>
